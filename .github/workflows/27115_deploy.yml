name: Test Workflow
on:
  push:
    branches:
      - main

jobs:
  check-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        
      - name: Check if test scripts exist
        run: |
          if [ ! -d "tests" ]; then
            echo "Test scripts directory not found" > napaka.txt
            exit 1
          fi
        
      - name: Prepare test environment
        run: |
          # Setup test environment here
        
      - name: Run tests
        run: |
          # Run tests here
        
      - name: Store artifacts
        if: always()
        uses: actions/upload-artifact@v2
        with:
          name: test-results
          path: tests/results
  
  handle-error:
    runs-on: ubuntu-latest
    needs: check-tests
    if: failure()
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        
      - name: Read error file
        run: cat napaka.txt
